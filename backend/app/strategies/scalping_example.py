"""Example scalping strategy following the user's rules — skeleton only.
Implementations should use real OHLCV streams and indicator libraries (ta-lib, pandas-ta).
"""
from typing import Dict

def scalping_signal(candle: Dict) -> Dict:
    # Placeholder logic; real implementation should compute EMA9, EMA20, VWAP, RSI, volume
    # and return structured signal when conditions met.
    return None
